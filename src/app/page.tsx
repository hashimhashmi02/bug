import { Button } from "@/components/ui/button";

const page = () => {
  return (
    <div className="text-red-300 display-flex justify-center items-center">
      <Button variant="outline" size="lg" className="rounded-full">NOOR</Button>
    </div>
  )
}

export default page;